---
title: "Geïntegreerd project Web 3 en Cloudontwikkeling"
sidebar_label: "Web 3 en Cloudontwikkeling"
sidebar_position: 2
---

## Inleiding

Neem je Cloudontwikkeling en Web 3 in hetzelfde semester op, dan werk je aan **één geïntegreerd fullstackproject** voor beide OLOD’s.

In Cloudontwikkeling bouw je een C#-backend met verschillende services. Voor Web 3 bouw je hierop een volwaardige frontend in React. Door beide OLOD’s in één product te combineren, kan je meer aandacht besteden aan de kwaliteit en samenhang van je applicatie.

## Doelstellingen

Met dit project toon je aan dat je:

- een volwaardige React-frontend kan bouwen die communiceert met een C#-backend;
- kan werken met context, state, routing en formulieren;
- zoeken, filteren en pagineren op serverniveau kan verwerken in de frontend;
- een gesimuleerde betalingsflow kan integreren;
- frontend en backend afzonderlijk kan deployen met een correcte CORS-configuratie;
- de CI/CD-richtlijnen uit Cloudontwikkeling kan toepassen;
- via Classroom50 een duidelijke commitgeschiedenis kan opbouwen;
- alle onderdelen van de applicatie kan verdedigen en toelichten.

## Functionele vereisten

### Data en CRUD

- Gebruik meerdere entiteiten met minstens één 1-N- of N-N-relatie uit het project van Cloudontwikkeling.
- Voorzie per relevante entiteit een volledige CRUD-flow: lijst, detail, toevoegen, bewerken en verwijderen.
- Gebruik formulieren met duidelijke validatie en foutmeldingen.

### Authenticatie en rollen

- Implementeer registratie en inloggen volgens de richtlijnen van Cloudontwikkeling.
- Gebruik minstens twee rollen, bijvoorbeeld `user` en `admin`.
- Pas de gebruikersinterface en toegelaten acties aan volgens de rol van de gebruiker.

### Zoeken en betalingen

- Voorzie zoeken, filteren en pagineren op serverniveau en toon de resultaten correct in de frontend.
- Implementeer een volledige gesimuleerde betaalflow met Mollie of Stripe.
- Verwerk de bijhorende webhook in de backend.

### Gebruikerservaring

- Gebruik minstens één niet-triviale React context, bijvoorbeeld voor authenticatie, een winkelmandje of permissions.
- Voorzie duidelijke toestanden voor `loading`, `error`, `empty` en `success`.
- Zorg voor een consistente layout en verzorgde styling.
- Voorzie minstens één exportfunctie, bijvoorbeeld naar CSV of PDF.

## Technische vereisten

- React met TypeScript en Vite;
- geen gebruik van `any`;
- React Router;
- React Hook Form of Formik;
- Fetch API of Axios met foutafhandeling;
- de C#-backend uitbreiden waar nodig, met correcte HTTP-statuscodes en security;
- frontend en backend afzonderlijk deployen;
- CORS correct configureren;
- Classroom50 voor de repository en opvolging door de lectoren;
- CI/CD volgens de richtlijnen van Cloudontwikkeling;
- een duidelijke en regelmatige commitgeschiedenis.

## Verdediging

Aan het einde volgt een individuele verdediging van ongeveer **40 minuten** voor Web 3 en Cloudontwikkeling samen. Je licht de architectuur toe, legt geselecteerde code uit en voert mogelijk een live aanpassing uit.

<aside className="courseCallout courseCallout--info">

<strong className="courseCallout__title">Twee afzonderlijke OLOD’s</strong>

Hoewel je één geïntegreerd project maakt, worden Web 3 en Cloudontwikkeling afzonderlijk beoordeeld. Je krijgt voor ieder OLOD een eigen eindresultaat. Een onvoldoende voor het ene OLOD verhindert niet dat je voor het andere OLOD kan slagen.
</aside>

De tweede examenkans vindt plaats in **augustus**. De lectoren delen mee welke opdracht en oplevering voor het betreffende OLOD verwacht worden.
